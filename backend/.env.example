# Copy to .env (git-ignored) and fill in. Loaded with:
#   .venv/bin/uvicorn app.main:app --reload --env-file .env

# Mood classification (Claude). Create at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Supabase project "Uns" (public values; the publishable key is safe to ship).
SUPABASE_URL=https://yjyegwckgrxwtoqygsrm.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_hNj94AJ_dqwAo_G7sMf5rQ_FefPaDrJ

# Optional: RevenueCat secret API key (v1), so account deletion also
# removes the purchase record. Project settings → API keys → Secret.
REVENUECAT_SECRET_KEY=
